# DEPLOY
  - Copiar todo codigo fonte da pasta ~/djangodev/logusweb para ./codigo
  - Editar arquivo ./codigo/logusweb/settings.py
    - Mudar host do banco de dados para: db (link container postgres)
    - Remover comentario da linha STATIC_ROOT
  - Rodar: ./logus



# ENTRAR NO BASH DO CONTAINER
  - docker ps 
    (pegar ID do container)
  - docker exec -it 987c39189286 /bin/bash
    (exit para sair)


# Medir a performance do Site (Ferramenta ApacheBench ou AB)
  - apt-get install apache2-utils

  - ab -c 10 -n 1000 http://127.0.0.1:8000/static/favicon/favicon.ico

    -c = numero de conexoes
    -n = numero de requisicoes


# BACKUP / RESTORE POSTGRESQL

Backup:
docker exec -t -u postgres your-db-container pg_dumpall -c > dump_`date +%d-%m-%Y"_"%H_%M_%S`.sql

Restore:
cat your_dump.sql | docker exec -i your-db-container psql -Upostgres


# GITHUB

  * Configuracao inicial:
    - git init
    - git ????

  * Manutencao:
    - git add arquivo     OU      git add .     (para todos)
      - git rm arquivo 
    - git commit -m 'meu commit'
    - git push origin master


# POPULAR BANCO DE DADOS POSTGRES (ADMINER)
  -INSERT INTO logus_fornecedor (nome,email,fone1,fone2,tipo,data_pub)
VALUES ('Fornecedor '||generate_series(1,1000000),'1','1','1','J',current_timestamp);
